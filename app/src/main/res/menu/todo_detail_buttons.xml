<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_edit_todo"
        android:title="Bearbeiten"
        android:visible="true"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_delete_todo"
        android:title="Löschen"
        android:visible="false"
        app:showAsAction="ifRoom"/>


    <item
        android:id="@+id/action_save_todo"
        android:title="Speichern"
        android:visible="false"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_cancel_todo"
        android:title="Abbrechen"
        android:visible="false"
        app:showAsAction="ifRoom"/>

</menu>